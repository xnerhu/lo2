import * as React from 'react';

import { MenuContainer, MenuItem } from '..';

export const AboutMenu = () => {
  return (
    <MenuContainer content='about'>
      <MenuItem>
        Open
       </MenuItem>
    </MenuContainer>
  );
};
